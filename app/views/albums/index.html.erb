<div class="albums_index">

<table>
  <tbody>
    <% @albums.each do |album| -%>
      <tr>
        <td><%= album.title %></td>
        <td><%= album.artist&.name %></td>
      </tr>
    <% end -%>
  </tbody>
</table>

</div>
